@{
    Layout = "_Layout";
    if (User.IsInRole(UserRoleInfo.Admin) 
    || User.IsInRole(UserRoleInfo.Doctor)
    || User.IsInRole(UserRoleInfo.Receptionist)
    )
    { 
        Layout = "_LayoutAdmin";
    }
}
